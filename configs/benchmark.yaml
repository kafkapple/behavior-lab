defaults:
  - _self_

paths:
  data_dir: ${oc.env:BEHAVIOR_LAB_DATA,data}
  output_dir: ${oc.env:BEHAVIOR_LAB_OUTPUT,outputs}

benchmark:
  algorithms:
    - bsoid
    - moseq
    - subtle
  datasets:
    - calms21
    - rat7m
  seed: 42
  n_jobs: 1
